.DS_Store
__pycache__/
*.pyc

# Build output — never committed. Do not rename these; the leading
# underscore is what keeps the folder out of git.
_phase2/
_preview/
preview/
Preview/

# GUEST DATA — this repo is PUBLIC (GitHub Pages serves it). The guest list
# lives in the Google Sheet and is read server-side by the Apps Script.
# Never commit names, emails or codes here.
guests*.csv
guest*.csv
*guest-list*
*guestlist*
rsvp-export*.csv
responses*.csv
*.xlsx
